# Пример файла переменных окружения
# Скопируйте этот файл в .env и заполните реальными значениями
# Команда: cp env.example.txt .env

# Токен Telegram бота (получите у @BotFather)
TELEGRAM_TOKEN=your_telegram_bot_token_here

# URL подключения к SQLite (по умолчанию)
# Файл БД будет создан автоматически при первом запуске
# Относительный путь: sqlite+aiosqlite:///./bot.db
# Абсолютный путь: sqlite+aiosqlite:////opt/parsertaxi/bot.db
# Для Windows: sqlite+aiosqlite:///C:/path/to/bot.db
DATABASE_URL=sqlite+aiosqlite:///./bot.db

# Опционально: ключ API для DeepSeek (если используется)
# DEEPSEEK_API_KEY=your_deepseek_api_key_here

# ID владельца бота (owner) - будет автоматически установлен при первом запуске
# Узнать свой ID можно через бота @userinfobot
OWNER_ID=your_telegram_user_id_here

